server:
  port: 8888 
  
management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: /actuator  
  endpoint:
    health:
      show-details: always

spring:
  autoconfigure:
    exclude: 
      - org.springframework.boot.autoconfigure.thymeleaf.ThymeleafAutoConfiguration

      
 